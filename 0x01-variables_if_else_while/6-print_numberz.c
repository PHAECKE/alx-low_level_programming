#include <time.h>
#include <stdlib.h>
#include <stdio.h>
/**
 * main - is the entry point
 * Return: is 0 after compiling
 */
int main(void)
{
	int f;

	for (f = '0'; f <= '9'; f++)
		putchar(d);
	putchar('\n');
	return (0);
}
