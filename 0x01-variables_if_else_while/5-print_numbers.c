#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - is the entry point
 * Return: is 0 after compiling
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
		printf("%d", i);
	printf("\n");
	return (0);
}
